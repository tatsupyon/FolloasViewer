# 変更履歴 (FolloasViewer / FolloasConverter)

| No. | 日付 | バージョン | カテゴリ | 対象箇所 | 問題点 / 変更理由 | 修正点 / 対応内容 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 2026/09/13 | V1.65(Viewer)/V1.36(Converter) | 仕様変更 | 全般、AGENTS.md、ドキュメント類 | ドキュメントの保守性と閲覧性の向上のため、PDF管理を廃止。 | マニュアルおよび仕様書の`.pdf`を廃止し、`.md`から生成した`.html`による管理へ移行。また`.agents/AGENTS.md`にGit運用およびドキュメントルールを追記。 |
| 2 | 2026/09/13 | V1.65(Viewer)/V1.36(Converter) | 機能改善 | FolloasConverter.py, FolloasViewer.py | コマンドライン実行時に引数のヘルプ表示手段が存在せず、未知の引数がパスとして誤認されていた。 | `-h`, `--help` オプションに対応し、起動時のコンソールに利用方法を出力して終了する処理を追加。 |
| 3 | 2026/09/13 | V1.36(Converter) | 機能追加 | FolloasConverter.py, Manual, Spec | カメラの逆さ設置時等に対応するため、抽出画像と認識ログの180度反転出力が必要になった。 | GUIに「180度回転」チェックボックスを追加し、`-rotate` 起動引数に対応。ONの場合、FFmpegによる画像反転出力(`vflip, hflip`)およびログ座標の反転処理(`720 - (x+w)`)を追加。 |
| 4 | 2026/09/13 | V1.66(Viewer) | 機能追加 | FolloasViewer.py | Live2映像の横圧縮をなくし、自然なアスペクト比で表示したい。 | 起動引数 `-no-compress-l2` と「LIVE2圧縮なし」UIを追加。Live2画像をクロップし横方向へ引き伸ばして表示するよう改修。 |
| 5 | 2026/09/13 | V1.67(Viewer) | 機能追加 | FolloasViewer.py | Live2画像を学習用としてYOLO形式アノテーションと共に出力したい。また、位相合わせを容易にしたい。 | 「学習画像エクスポート」機能（画像クロップとYOLO形式.txt生成）と、「ログ固定(Lock)」機能（コマ送りに連動したオフセット自動計算）を追加。 |
