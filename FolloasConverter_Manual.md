# Folloas データコンバータ 操作マニュアル
対応バージョン: V1.09〜
動作環境: Python 3.14.4



## 1. はじめに
本ツールは、録画された重い動画ファイル（TSやMKV）と認識ログデータを、Viewerで高速にシーク・同期再生させるための「連番静止画データ（`_ViewReady`）」に事前変換するツールです。

## 2. 操作手順
1. **ツールの起動:** `FolloasConverter.py` を起動します。
2. **対象の選択:** 「処理対象フォルダ」の **[参照]** ボタンをクリックし、録画データが入っているフォルダを選択します。
    * フォルダ内には `*_vlc_record_live1.ts`, `*_vlc_record_live2.ts`, `*_screen_capture.mkv`, `*_head.log` の4点が揃っている必要があります。
3. **変換の実行:** **[変換開始]** ボタンをクリックします。
    * プログレスバーが進行し、現在の処理フェーズが表示されます。
    * **日本語フォルダ名**や**全角文字**を含むパスも問題なく処理できます。
4. **完了確認:** 「変換完了！」というメッセージが表示されたら完了です。
    * 元のフォルダと同じ場所に **「元のフォルダ名_ViewReady」** というフォルダが作成されます。このフォルダを `FolloasViewer` で開いてください。

## 3. 高度な機能と注意点
* **データの整合性維持**: 
    * 万が一ログデータが途中で途切れていても、時間を理論値（30fps一定レート）で補完しながら変換を継続します。これによりViewerでの同期がズレるのを防ぎます。
* **最新ログ形式への対応**:
    * AIが出力するログにヘッダー行（項目の説明行）がない場合でも、自動的にデータを判別して変換を行います。
* **中断（キャンセル）**:
    * 処理中に **[キャンセル]** を押すと安全に終了します。途中で生成されたデータは不完全なため、必ず最初からやり直してください。